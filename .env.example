# Environment variables for Pro Project

# SECURITY - MUST CHANGE IN PRODUCTION
SECRET_KEY=change-me-32-bytes-hex-here
SECURITY_PASSWORD_SALT=change-me-too-32-bytes-hex-here

# Google OAuth (for Google Sign-In)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/proproject

# Flask Environment
FLASK_ENV=development
FLASK_APP=run.py

# Mail Configuration
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USE_SSL=false
MAIL_PROVIDER=smtp
MAIL_FROM_NAME="Pro Project"
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=<APP_PASSWORD_GENERATED_BY_GOOGLE>
# MAIL_DEFAULT_SENDER is the email address shown as sender
MAIL_DEFAULT_SENDER=noreply@proproject.com

# If using Gmail SMTP with a personal account, create an App Password
# at https://myaccount.google.com/security -> App passwords (requires 2FA)

# Redis for rate limiting (optional)
REDIS_URL=redis://localhost:6379/0

# Generate secret keys with:
# python -c "import secrets; print(secrets.token_hex(32))"